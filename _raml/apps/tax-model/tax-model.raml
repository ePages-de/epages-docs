#%RAML 0.8
displayName: Tax model
get:
  description: Returns the tax model of the shop. *epages6 *epagesNow
  securedBy: [products_read]
  responses:
    200:
      body:
        application/json:
          example: !include examples/get_taxmodel_response.json
          schema: !include schema/get_taxmodel.schema.json
