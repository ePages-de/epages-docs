#%RAML 0.8
displayName: Shop countries
get:
  description: Returns all supported countries of the shop, i.e. all countries that can be selected as delivery countries for orders placed in the shop. *epages6 *epagesNow
  responses:
    200:
      body:
        application/json:
          example: !include examples/get_countries_response.json
          schema: !include schema/get_countries.schema.json
