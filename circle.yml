machine:
  ruby:
    version: 2.2.0
  services:
    - docker
  environment:
    DOCKER_IMAGE_ELASTICSEARCH: epages/docs-elasticsearch
    DOCKER_IMAGE_NGINX: epages/docs-nginx
    DOCKER_IMAGE_TAG: ${CIRCLE_BRANCH//\//-}

test:
  override:
    - ./build.sh "${DOCKER_IMAGE_TAG}"

deployment:
  push:
    branch:
      - master
      - develop
    commands:
      - docker login -u "${DOCKER_USERNAME}" -p "${DOCKER_PASSWORD}" -e "${DOCKER_EMAIL}"
      - docker push "${DOCKER_IMAGE_ELASTICSEARCH}:${DOCKER_IMAGE_TAG}"
      - docker push "${DOCKER_IMAGE_NGINX}:${DOCKER_IMAGE_TAG}"
